<%= render "devise/shared/header" %>

<%= render "devise/shared/side_bar", book: @booknew, user: @userself %>

		<div class="col-lg-8">
			<h1>Books</h1>
			<table class = "table">
				<tr>
				<th></th>
				<th>Title</th>
				<th>Opinion</th>
			</tr>
			<% @books.each do | book | %>
			<tr>
				<td>
					<%= attachment_image_tag @users, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %>
				</td>
				<td>
					<%= link_to book.title,book %>
				</td>
				<td><%= book.body %></td>
			</tr>
			<% end %>
		</table>

		</div>
	</div>
</div>