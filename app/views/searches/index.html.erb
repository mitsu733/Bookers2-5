
<%= render "devise/shared/side_bar", book: @booknew, user: @users %>

		<div class="col-xs-9">
			<h1>Users</h1>
			<table class = "table table-hover" >
				<tr>
				<th></th>
				<th>Title</th>
				<th>Opinion</th>
				</tr>
				<% @searches.each do | search | %>
				<tr>
				<td>
					<%= link_to user_path(search) do %>
					<%= attachment_image_tag search, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %>
					<% end %>
				</td>
				<td><%= link_to search.name, user_path(search) %></td>
				<td><%= search.introduction %></td>
				</tr>
				<%end%>
				</table>
		</div>